# Real World Use Cases

Examples from production applications

---

## WebRTC Video Chat

Built a real-time collaboration tool using:

- `RTCPeerConnection` for peer connections
- `MediaStream` for audio/video tracks
- `getUserMedia()` for device access

[Try the WebRTC Demo →](/api/webrtc-webcodecs)

---

## Audio Visualization

Created an audio visualizer using:

- `AudioContext` for audio processing
- `AnalyserNode` for frequency data
- `Canvas` for rendering

[Try the AudioContext Demo →](/api/audiocontext)

---

## Screen Recording

Implemented screen recording with:

- `MediaRecorder` API
- `getDisplayMedia()` for screen capture
- Blob handling for downloads

[Try the Media Recorder Demo →](/api/media-recorder)
